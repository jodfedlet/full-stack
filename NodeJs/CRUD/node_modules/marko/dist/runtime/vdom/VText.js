var VNode = require("./VNode");
var inherit = require("raptor-util/inherit");

function VText(value) {
    this.bt_(-1 /* no children */);
    this.aF_ = value;
}

VText.prototype = {
    bH_: true,

    aA_: 3,

    az_: function (doc) {
        return doc.createTextNode(this.aF_);
    },

    bn_: function () {
        return new VText(this.aF_);
    }
};

inherit(VText, VNode);

module.exports = VText;
"use strict";
/* jshint newcap:false */

var complain;

var domInsert = require("../runtime/dom-insert");
var defaultCreateOut = require("../runtime/createOut");
var getComponentsContext = require("./ComponentsContext").__;
var componentsUtil = require("./util");
var componentLookup = componentsUtil.a_;
var emitLifecycleEvent = componentsUtil.b_;
var destroyNodeRecursive = componentsUtil.c_;
var EventEmitter = require("events-light");
var RenderResult = require("../runtime/RenderResult");
var SubscriptionTracker = require("listener-tracker");
var inherit = require("raptor-util/inherit");
var updateManager = require("./update-manager");
var morphdom = require("../morphdom");
var eventDelegation = require("./event-delegation");

var slice = Array.prototype.slice;

var COMPONENT_SUBSCRIBE_TO_OPTIONS;
var NON_COMPONENT_SUBSCRIBE_TO_OPTIONS = {
    addDestroyListener: false
};

var emit = EventEmitter.prototype.emit;
var ELEMENT_NODE = 1;

function removeListener(removeEventListenerHandle) {
    removeEventListenerHandle();
}

function handleCustomEventWithMethodListener(component, targetMethodName, args, extraArgs) {
    // Remove the "eventType" argument
    args.push(component);

    if (extraArgs) {
        args = extraArgs.concat(args);
    }

    var targetComponent = componentLookup[component.d_];
    var targetMethod = typeof targetMethodName === "function" ? targetMethodName : targetComponent[targetMethodName];
    if (!targetMethod) {
        throw Error("Method not found: " + targetMethodName);
    }

    targetMethod.apply(targetComponent, args);
}

function resolveKeyHelper(key, index) {
    return index ? key + "_" + index : key;
}

function resolveComponentIdHelper(component, key, index) {
    return component.id + "-" + resolveKeyHelper(key, index);
}

/**
 * This method is used to process "update_<stateName>" handler functions.
 * If all of the modified state properties have a user provided update handler
 * then a rerender will be bypassed and, instead, the DOM will be updated
 * looping over and invoking the custom update handlers.
 * @return {boolean} Returns true if if the DOM was updated. False, otherwise.
 */
function processUpdateHandlers(component, stateChanges, oldState) {
    var handlerMethod;
    var handlers;

    for (var propName in stateChanges) {
        if (stateChanges.hasOwnProperty(propName)) {
            var handlerMethodName = "update_" + propName;

            handlerMethod = component[handlerMethodName];
            if (handlerMethod) {
                (handlers || (handlers = [])).push([propName, handlerMethod]);
            } else {
                // This state change does not have a state handler so return false
                // to force a rerender
                return;
            }
        }
    }

    // If we got here then all of the changed state properties have
    // an update handler or there are no state properties that actually
    // changed.
    if (handlers) {
        // Otherwise, there are handlers for all of the changed properties
        // so apply the updates using those handlers

        handlers.forEach(function (handler) {
            var propertyName = handler[0];
            handlerMethod = handler[1];

            var newValue = stateChanges[propertyName];
            var oldValue = oldState[propertyName];
            handlerMethod.call(component, newValue, oldValue);
        });

        emitLifecycleEvent(component, "update");

        component.e_();
    }

    return true;
}

function checkInputChanged(existingComponent, oldInput, newInput) {
    if (oldInput != newInput) {
        if (oldInput == null || newInput == null) {
            return true;
        }

        var oldKeys = Object.keys(oldInput);
        var newKeys = Object.keys(newInput);
        var len = oldKeys.length;
        if (len !== newKeys.length) {
            return true;
        }

        for (var i = 0; i < len; i++) {
            var key = oldKeys[i];
            if (oldInput[key] !== newInput[key]) {
                return true;
            }
        }
    }

    return false;
}

var componentProto;

/**
 * Base component type.
 *
 * NOTE: Any methods that are prefixed with an underscore should be considered private!
 */
function Component(id) {
    EventEmitter.call(this);
    this.id = id;
    this.f_ = null;
    this.g_ = null;
    this.h_ = null;
    this.i_ = null;
    this.j_ = null; // Used to keep track of bubbling DOM events for components rendered on the server
    this.k_ = null;
    this.d_ = null;
    this.l_ = null;
    this.m_ = undefined;
    this.n_ = false;
    this.o_ = undefined;

    this.p_ = false;
    this.q_ = false;
    this.r_ = false;
    this.s_ = false;

    this.t_ = undefined;

    this.u_ = {};
    this.v_ = undefined;
}

Component.prototype = componentProto = {
    w_: true,

    subscribeTo: function (target) {
        if (!target) {
            throw TypeError();
        }

        var subscriptions = this.h_ || (this.h_ = new SubscriptionTracker());

        var subscribeToOptions = target.w_ ? COMPONENT_SUBSCRIBE_TO_OPTIONS : NON_COMPONENT_SUBSCRIBE_TO_OPTIONS;

        return subscriptions.subscribeTo(target, subscribeToOptions);
    },

    emit: function (eventType) {
        var customEvents = this.k_;
        var target;

        if (customEvents && (target = customEvents[eventType])) {
            var targetMethodName = target[0];
            var isOnce = target[1];
            var extraArgs = target[2];
            var args = slice.call(arguments, 1);

            handleCustomEventWithMethodListener(this, targetMethodName, args, extraArgs);

            if (isOnce) {
                delete customEvents[eventType];
            }
        }

        if (this.listenerCount(eventType)) {
            return emit.apply(this, arguments);
        }
    },
    getElId: function (key, index) {
        return resolveComponentIdHelper(this, key, index);
    },
    getEl: function (key, index) {
        if (key) {
            var resolvedKey = resolveKeyHelper(key, index);
            var keyedElement = this.u_["@" + resolvedKey];

            if (!keyedElement) {
                var keyedComponent = this.getComponent(resolvedKey);

                if (keyedComponent) {
                    return keyedComponent.g_.firstChild;
                    // eslint-disable-next-line no-constant-condition
                }
            }

            return keyedElement;
        } else {
            return this.g_ && this.g_.firstChild;
        }
    },
    getEls: function (key) {
        key = key + "[]";

        var els = [];
        var i = 0;
        var el;
        while (el = this.getEl(key, i)) {
            els.push(el);
            i++;
        }
        return els;
    },
    getComponent: function (key, index) {
        var rootNode = this.u_[resolveKeyHelper(key, index)];
        if (/\[\]$/.test(key)) {
            rootNode = rootNode && rootNode[Object.keys(rootNode)[0]];
            // eslint-disable-next-line no-constant-condition
        }
        return rootNode && rootNode.x_;
    },
    getComponents: function (key) {
        var lookup = this.u_[key + "[]"];
        return lookup ? Object.keys(lookup).map(function (key) {
            return lookup[key].x_;
        }) : [];
    },
    destroy: function () {
        if (this.p_) {
            return;
        }

        var root = this.g_;
        var nodes = this.g_.nodes;

        this.y_();

        nodes.forEach(function (node) {
            destroyNodeRecursive(node);

            if (eventDelegation.z_(node) !== false) {
                node.parentNode.removeChild(node);
            }
        });

        root.detached = true;

        delete componentLookup[this.id];
    },

    y_: function () {
        if (this.p_) {
            return;
        }

        emitLifecycleEvent(this, "destroy");
        this.p_ = true;

        this.g_.x_ = undefined;

        this.g_ = null;

        // Unsubscribe from all DOM events
        this.A_();

        var subscriptions = this.h_;
        if (subscriptions) {
            subscriptions.removeAllListeners();
            this.h_ = null;
        }
    },

    isDestroyed: function () {
        return this.p_;
    },
    get state() {
        return this.f_;
    },
    set state(newState) {
        var state = this.f_;
        if (!state && !newState) {
            return;
        }

        if (!state) {
            state = this.f_ = new this.B_(this);
        }

        state.C_(newState || {});

        if (state.r_) {
            this.D_();
        }

        if (!newState) {
            this.f_ = null;
        }
    },
    setState: function (name, value) {
        var state = this.f_;

        if (typeof name == "object") {
            // Merge in the new state with the old state
            var newState = name;
            for (var k in newState) {
                if (newState.hasOwnProperty(k)) {
                    state.E_(k, newState[k], true /* ensure:true */);
                }
            }
        } else {
            state.E_(name, value, true /* ensure:true */);
        }
    },

    setStateDirty: function (name, value) {
        var state = this.f_;

        if (arguments.length == 1) {
            value = state[name];
        }

        state.E_(name, value, true /* ensure:true */
        , true /* forceDirty:true */
        );
    },

    replaceState: function (newState) {
        this.f_.C_(newState);
    },

    get input() {
        return this.m_;
    },
    set input(newInput) {
        if (this.s_) {
            this.m_ = newInput;
        } else {
            this.F_(newInput);
        }
    },

    F_: function (newInput, onInput, out) {
        onInput = onInput || this.onInput;
        var updatedInput;

        var oldInput = this.m_;
        this.m_ = undefined;

        if (onInput) {
            // We need to set a flag to preview `this.input = foo` inside
            // onInput causing infinite recursion
            this.s_ = true;
            updatedInput = onInput.call(this, newInput || {}, out);
            this.s_ = false;
        }

        newInput = this.l_ = updatedInput || newInput;

        if (this.r_ = checkInputChanged(this, oldInput, newInput)) {
            this.D_();
        }

        if (this.m_ === undefined) {
            this.m_ = newInput;
            if (newInput && newInput.$global) {
                this.o_ = newInput.$global;
            }
        }

        return newInput;
    },

    forceUpdate: function () {
        this.r_ = true;
        this.D_();
    },

    D_: function () {
        if (!this.q_) {
            this.q_ = true;
            updateManager.G_(this);
        }
    },

    update: function () {
        if (this.p_ === true || this.H_ === false) {
            return;
        }

        var input = this.m_;
        var state = this.f_;

        if (this.r_ === false && state !== null && state.r_ === true) {
            if (processUpdateHandlers(this, state.I_, state.J_, state)) {
                state.r_ = false;
            }
        }

        if (this.H_ === true) {
            // The UI component is still dirty after process state handlers
            // then we should rerender

            if (this.shouldUpdate(input, state) !== false) {
                this.K_(false);
            }
        }

        this.e_();
    },

    get H_() {
        return this.r_ === true || this.f_ !== null && this.f_.r_ === true;
    },

    e_: function () {
        this.r_ = false;
        this.q_ = false;
        this.l_ = null;
        var state = this.f_;
        if (state) {
            state.e_();
        }
    },

    shouldUpdate: function () {
        return true;
    },

    b_: function (eventType, eventArg1, eventArg2) {
        emitLifecycleEvent(this, eventType, eventArg1, eventArg2);
    },

    K_: function (isRerenderInBrowser) {
        var self = this;
        var renderer = self.L_;

        if (!renderer) {
            throw TypeError();
        }

        var rootNode = this.g_;

        var doc = self.t_;
        var input = this.l_ || this.m_;
        var globalData = this.o_;

        updateManager.M_(function () {
            var createOut = renderer.createOut || defaultCreateOut;
            var out = createOut(globalData);
            out.sync();
            out.t_ = self.t_;

            var componentsContext = getComponentsContext(out);
            var globalComponentsContext = componentsContext.N_;
            globalComponentsContext.O_ = self;
            globalComponentsContext.P_ = isRerenderInBrowser;

            renderer(input, out);

            var result = new RenderResult(out);

            var targetNode = out.Q_().R_;

            morphdom(rootNode, targetNode, doc, componentsContext);

            result.afterInsert(doc);
        });

        this.e_();
    },

    S_: function () {
        var root = this.g_;
        root.remove();
        return root;
    },

    A_: function () {
        var eventListenerHandles = this.i_;
        if (eventListenerHandles) {
            eventListenerHandles.forEach(removeListener);
            this.i_ = null;
        }
    },

    get T_() {
        var state = this.f_;
        return state && state.U_;
    },

    V_: function (customEvents, scope) {
        var finalCustomEvents = this.k_ = {};
        this.d_ = scope;

        customEvents.forEach(function (customEvent) {
            var eventType = customEvent[0];
            var targetMethodName = customEvent[1];
            var isOnce = customEvent[2];
            var extraArgs = customEvent[3];

            finalCustomEvents[eventType] = [targetMethodName, isOnce, extraArgs];
        });
    },

    get el() {
        return this.g_ && this.g_.firstChild;
        // eslint-disable-next-line no-constant-condition
    },

    get els() {
        return (this.g_ ? this.g_.nodes : []).filter(function (el) {
            return el.nodeType === ELEMENT_NODE;
        });
        // eslint-disable-next-line no-constant-condition
    }
};

componentProto.elId = componentProto.getElId;
componentProto.W_ = componentProto.update;
componentProto.X_ = componentProto.destroy;

// Add all of the following DOM methods to Component.prototype:
// - appendTo(referenceEl)
// - replace(referenceEl)
// - replaceChildrenOf(referenceEl)
// - insertBefore(referenceEl)
// - insertAfter(referenceEl)
// - prependTo(referenceEl)
domInsert(componentProto, function getEl(component) {
    return component.S_();
}, function afterInsert(component) {
    return component;
});

inherit(Component, EventEmitter);

module.exports = Component;
"use strict";

class ServerComponent {
    constructor(id, input, out, typeName, customEvents, scope) {
        this.id = id;
        this.k_ = customEvents;
        this.d_ = scope;
        this._C_ = undefined;
        this.m_ = undefined;
        this.f_ = undefined;
        this.typeName = typeName;
        this.j_ = undefined; // Used to keep track of bubbling DOM events for components rendered on the server
        this._D_ = 0;

        if (this.onCreate !== undefined) {
            this.onCreate(input, out);
        }

        if (this.onInput !== undefined) {
            var updatedInput = this.onInput(input, out) || input;

            if (this.m_ === undefined) {
                this.m_ = updatedInput;
            }

            this._C_ = updatedInput;
        } else {
            this.m_ = this._C_ = input;
        }

        if (this.onRender !== undefined) {
            this.onRender(out);
        }
    }

    set input(newInput) {
        this.m_ = newInput;
    }

    get input() {
        return this.m_;
    }

    set state(newState) {
        this.f_ = newState;
    }

    get state() {
        return this.f_;
    }

    get T_() {
        return this.f_;
    }

    elId(scopedId, index) {
        var id = this.id;

        var elId = scopedId != null ? id + "-" + scopedId : id;

        if (index != null) {
            elId += "[" + index + "]";
        }

        return elId;
    }
}

ServerComponent.prototype.getElId = ServerComponent.prototype.elId;

module.exports = ServerComponent;
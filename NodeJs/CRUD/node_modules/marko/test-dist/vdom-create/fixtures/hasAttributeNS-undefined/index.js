var expect = require("chai").expect;

module.exports = function (helpers) {
    var virtualEl = helpers.vdom.createElement("option", {
        selected: undefined
    });

    expect(virtualEl.aI_("selected")).to.equal(false);

    return virtualEl;
};
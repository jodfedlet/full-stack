var expect = require("chai").expect;

module.exports = function (helpers) {
    var virtualEl = helpers.vdom.createElement("option", { selected: 0 });

    expect(virtualEl.aI_("selected")).to.equal(true);

    return virtualEl;
};